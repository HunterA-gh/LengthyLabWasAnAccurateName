import java.util.Arrays;

public class TestPeople {

    public static void main(String[] args) {
        /*      ***Class is now abstract***
        People pl1 = new People();
        Person p1 = new Person(1001, "John");
        Person p2 = new Person(1002, "Joe");
        Person p3 = new Person(1003, "Jack");

        testAdd(pl1, p1);
        testAdd(pl1, p2);
        testAdd(pl1, p3);

        testRemove(pl1, p2);

    }

        static void testAdd(People pl, Person p){
            pl.addPerson(p);
            System.out.println(p.getName() + " has been added to the list. There is now " + pl.toArray().length + " in the list.");
        }

        static void testRemove(People pl, Person p){
            pl.remove(p);
            System.out.println(p.getName() + " has been removed to the list. There is now " + pl.toArray().length + " in the list.");
        }

         */

    }
}